# Grafana Dashboard Provisioning Configuration
apiVersion: 1

providers:
  - name: 'Python Template Server Dashboards'
    orgId: 1
    folder: 'Python Template Server'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
